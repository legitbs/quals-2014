100lines is a reversing challenge, a key expanded array of size (stupid_big)TBs is used to xor with the bytes of the key.  The solution is to build the bytes without creating the aray.  

**There are two binaries, one for distibution to the teams (public) and one which is optimize for running on our server (LBS).  


